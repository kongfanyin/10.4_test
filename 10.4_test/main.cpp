#define _CRT_SECURE_NO_WARNINGS 1
//编写一个程序，它从栈中添加和customer结构（栈用Stack类声明表示）。每次customer 结构被删除时，其payment的值都被加入到总数中，并报告总数
//注意，应该可以使用Stack类而不做修改；只需修改typedef声明，使Item的类型为customer，而不是unsigned long 即可。

#include<string>
#include<fstream>
#include<iostream>

int main(char argc, char** argv)
{




	return 0;
}